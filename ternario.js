// const hora = 19

// // const saudacao = hora >= 12 ? "Boa tarde" : "Bom dia"
// // console.log(saudacao)

// const saudacao2 = hora < 11 ? "BOM DIA!" : (hora <= 18 ? "Boa tarde" : "Boa noite")
// console.log(saudacao2)

const a = 8
const b = 4
const operacao = '/'

const resultado = operacao === '+' ? a + b : 
                    operacao === '-' ? a + b :
                        operacao === '*' ? a * b : a / b
console.log(resultado)

